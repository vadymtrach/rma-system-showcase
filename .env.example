POSTGRES_DB=admin_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=secret_password
SPRING_PORT=8080

# Comma-separated browser origins allowed for the API and WebSocket (your public URL).
ALLOWED_ORIGINS=https://localhost
